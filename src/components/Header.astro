---
// Header.astro — Navigation principale Festicloze
---

<header class="flex items-center justify-between px-6 py-4 bg-primary">
    <!-- Logo + Nom -->
    <a href="/" class="flex items-center gap-2">
        <img src="/logo.svg" alt="Festicloze" class="h-10 w-10" />
        <span class="font-heading text-cream text-xl">Festicloze</span>
    </a>

    <!-- Bouton burger (mobile) -->
    <button
        id="menu-toggle"
        class="text-cream md:hidden"
        aria-label="Ouvrir le menu"
    >
        <svg
            id="icon-menu"
            xmlns="http://www.w3.org/2000/svg"
            class="h-8 w-8"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
        >
            <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
        <svg
            id="icon-close"
            xmlns="http://www.w3.org/2000/svg"
            class="h-8 w-8 hidden"
            fill="none"
            viewBox="0 0 24 24"
            stroke="currentColor"
        >
            <path
                stroke-linecap="round"
                stroke-linejoin="round"
                stroke-width="2"
                d="M6 18L18 6M6 6l12 12"></path>
        </svg>
    </button>

    <!-- Navigation desktop -->
    <nav class="hidden md:flex gap-6">
        <a
            href="/programme"
            class="text-cream hover:text-accent transition-colors">Programmes</a
        >
        <a
            href="/artistes"
            class="text-cream hover:text-accent transition-colors">Artistes</a
        >
        <a href="/scenes" class="text-cream hover:text-accent transition-colors"
            >Scènes</a
        >
        <a
            href="/a-propos"
            class="text-cream hover:text-accent transition-colors">À propos</a
        >
        <a
            href="/contact"
            class="text-cream hover:text-accent transition-colors">Contact</a
        >
        <a
            href="/infos-pratiques"
            class="text-cream hover:text-accent transition-colors"
            >Infos pratiques</a
        >
    </nav>
</header>

<!-- Menu mobile (plein écran) -->
<div
    id="mobile-menu"
    class="fixed inset-0 bg-primary z-50 flex-col items-start justify-center px-10 gap-8 hidden"
>
    <!-- Header du menu mobile -->
    <div
        class="absolute top-0 left-0 right-0 flex items-center justify-between px-6 py-4"
    >
        <a href="/" class="flex items-center gap-2">
            <img src="/logo.svg" alt="Festicloze" class="h-10 w-10" />
            <span class="font-heading text-cream text-xl">Festicloze</span>
        </a>
        <button id="menu-close" class="text-cream" aria-label="Fermer le menu">
            <svg
                xmlns="http://www.w3.org/2000/svg"
                class="h-8 w-8"
                fill="none"
                viewBox="0 0 24 24"
                stroke="currentColor"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M6 18L18 6M6 6l12 12"></path>
            </svg>
        </button>
    </div>

    <!-- Liens du menu mobile -->
    <nav class="flex flex-col gap-8 mt-24">
        <a
            href="/programme"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >Programmes</a
        >
        <a
            href="/artistes"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >Artistes</a
        >
        <a
            href="/scenes"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >Scènes</a
        >
        <a
            href="/a-propos"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >À propos</a
        >
        <a
            href="/contact"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >Contact</a
        >
        <a
            href="/infos-pratiques"
            class="font-heading text-cream text-4xl hover:text-accent transition-colors"
            >Infos pratiques</a
        >
    </nav>
</div>

<script>
    // @ts-nocheck
    const menuToggle = document.getElementById("menu-toggle");
    const menuClose = document.getElementById("menu-close");
    const mobileMenu = document.getElementById("mobile-menu");

    menuToggle.addEventListener("click", () => {
        mobileMenu.classList.remove("hidden");
        mobileMenu.classList.add("flex");
    });

    menuClose.addEventListener("click", () => {
        mobileMenu.classList.add("hidden");
        mobileMenu.classList.remove("flex");
    });
</script>
